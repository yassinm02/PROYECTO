package com.stockcontroll.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface TipoIvaRepository extends JpaRepository<com.stockcontroll.model.TipoIva, Integer> {
}
